export class TemporarilyClosed{
    public icon: string;
    public title: string;
    public dateRange: string;

    constructor(icon: string, title: string, dateRange: string){
        this.icon = icon;
        this.title = title;
        this.dateRange = dateRange;
    }
}
